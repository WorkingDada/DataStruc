#include <bits/stdc++.h>
int main(){
    ios_base::sync_with_stdio(false) ; cin.tie(0);
    int n,m ;
    size_t mFront,mSize,mCap,last;
    while (n--){
        std::cin >> mFront >> mSize >> mCap >> last;
        std::cin >> m;
        switch (m){
        case 0 :
            if 
            break;
        case 1 :
            break;
        case 2 :
            break;
        case 3 :
            break;
        case 4 :
            break;
        default:
            break;
        }
    }
}